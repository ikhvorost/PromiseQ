os: osx
osx_image: xcode11.3
language: swift
script: swift test --enable-test-discovery --enable-code-coverage
after_success: bash <(curl -s https://codecov.io/bash) -f $(swift test --show-codecov-path)
